'use strict';

angular.module('js.journeyman', ['utils']);
// Do this here (with the brackets '[]') so we can load the separate files
// "in no particular order"
angular.module('utils', []);
//angular.module('searchShared', []);
//angular.module('linkRecord', []);
